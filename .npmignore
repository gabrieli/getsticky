# npm uses .npmignore instead of .gitignore when this file exists.
# The "files" field in package.json whitelists what to include,
# so this file just needs to not exclude the build artifacts.

node_modules/
getsticky-app/
server/src/
server/node_modules/
.claude/
.git/
*.db
*.sqlite
getsticky-data/
